@import url('https://fonts.googleapis.com/css?family=Roboto');

.content
	height 100%
	width 100%
	display flex
	flex-direction column
	align-items center
	font-size 20px
	line-height 20px
	font-family 'Roboto'
	background #c7ecee
	.page
		width 100%
		height 100%
		display flex
		align-items center
		justify-content center